Interfaces die der Subserver benötigt vom Mainserver (Jonas)
- einen post('/try') ('/try/{word}/{correct word}' oder 'try/{word}/try-daily')
- post('/getNewWord')
- beim erstellen diese Argumente (gameId, Port, Name)

Was der Mainserver Managen muss (Jonas)
- Lobby erstellen (started subserver)
- Lobby beitreten (speicher z.b lokal welche lobbies offen sind, werde kreativ)
- Random wort erstellen/grabben

Interfaces die der Subserver benötigt vom Frontend/Logik (Jonathan, Kilian)
- Im game -> Submit knopf fetch mit ('/try/{word}/{correct word}' oder 'try/{word}/try-daily')
((-> bspl : socket.emit('guess', { gameId, guess, playerName });(nicht mehr akkurat)))
- script.js vom frontend handled die logik für die länge des wortes

Was der Main Server (WebController) von den Subservern braucht:
- Nachricht, wenn der Subserver gelöscht werden kann
- Schnittstellen sind in WebController.java zu finden





1v1:

2 Spielfelder (jeweiliger spieler)
Beim erstellen der lobby (in lobby.html) soll gefragt werden ob 1v1 oder team
updateBoard methode muss geupdated werden dafür
z.b. zwei listen user1 = {"guess1","guess2"...} user = {"guess","milch",...}
